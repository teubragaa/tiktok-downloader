services:

  tiktok-downloader:

    build: .

    container_name: tiktok-downloader

    ports:
      - "5000:5000"

    extra_hosts:
      - "host.docker.internal:host-gateway"

    restart: unless-stopped